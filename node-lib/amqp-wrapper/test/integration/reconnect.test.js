
describe('reconnect integration', () => {

  // going to need to send messages to running docker container to kill the amqp process.
  // assert that we get `close` events emitted
  // turn on the amqp process again
  // assert that we get 'connect' events and that channels are usable.

})
